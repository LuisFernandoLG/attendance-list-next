export type User = {
    id: number,
    name: string, 
    email: string,
    rol?: string,
    timezone: string,
    image_url?: string,
    created_at?: string,
    updated_at?: string,
    email_verified_at?: string;

}